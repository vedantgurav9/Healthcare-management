services:
  app:
    image: health-monitoring-system
    build: .
    ports:
      - "5000:5000"
    volumes:
      - patient_data:/tmp/data
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

volumes:
  patient_data:
